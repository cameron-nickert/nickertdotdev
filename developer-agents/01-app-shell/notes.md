# D01 App Shell Notes

- Anchored sections use canonical IDs: `home`, `who-i-am`, `story`, `work`, `resume`, `contact`.
- Sections include `data-scene` and `data-nav-section` for nav tracking.
- HUD controls removed per user request; navigation remains intact.
- Hash alias handling maps `#about` to `#who-i-am`.
