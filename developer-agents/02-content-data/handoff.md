Handoff
- Content schema added in `content/profile.ts` and expanded in `content/work.ts` with runtime validation.
- New `/work` listing at `app/work/page.tsx` and updated detail template at `app/work/[slug]/page.tsx`.
- Asset requests logged in `developer-agents/02-content-data/asset-requests.md`.
- Hero images moved into `public/images/work/<slug>/hero-01.png` and wired into case study hero sections.
